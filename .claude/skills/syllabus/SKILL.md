---
name: syllabus
description: 高専シラバスの作成・授業構成の検討スキル。MCCの内容をもとに授業計画を組み立て、エクセルファイルへの記入とMarkdownファイルへの出力を行う。シラバスの作成、授業構成の検討、MCCをもとにした授業設計、シラバスのエクセル記入など、授業計画に関連する作業全般で使用する。「シラバスを作りたい」「授業構成を考えたい」「MCCに沿った授業を設計したい」といった依頼にも対応する。
---

# シラバス作成スキル

高専のシラバスをエクセルテンプレートとMCC（モデルコアカリキュラム）をもとに作成するスキル。ユーザーが入力した基本情報・到達目標・ルーブリックから、授業計画・評価割合・概要などを生成し、エクセルとMarkdownの両方に出力する。

## なぜこのスキルが必要か

高専のシラバス作成は、6つのシートにまたがるエクセルファイルへの記入が必要で、MCCとの対応付けも求められる。この作業は毎年繰り返されるが、到達目標やルーブリックから授業計画を導き出すプロセスは構造的で、支援の余地が大きい。

## 前提条件

- 1コマの授業時間は **90分** とする
- Python3 がインストールされていること
- 初回実行時に依存パッケージをインストールする:
  ```bash
  pip3 install -r ${CLAUDE_SKILL_DIR}/requirements.txt
  ```
- エクセルテンプレートは6シート構成（後述）

## 対話ルール

ユーザーへの質問は場面に応じて適切な方式を使い分ける:

- **自由入力が自然な場面**（教員名、科目名、ファイルパス、到達目標、ルーブリックなど）: プレーンテキストで質問する
- **選択肢が明確な場面**（定期試験の種類、MCC対応の有無、既存情報の利用、授業の進め方など）: `AskUserQuestion` ツールを使う。自由入力は自動付与される "Other" で対応される

不明点や曖昧な点がある場合は、推測で進めずにユーザーに確認すること。

---

## 処理フロー

```
Step 0: 教員名・科目名の取得
Step 1: Excelファイルの指定
  ├→ Excel有り → Step 2a: 読み取り → Step 2b: 既存情報の利用確認
  └→ Excel無し → Step 2c: 対話で全情報収集
Step 3: 授業構成パラメータ（回数・授業構成案・評価スタイル・試験・MCC）
Step 4: MCC対応付け（オプション）
Step 5: 生成（概要・授業計画・評価割合・属性）
Step 6: プレビュー・修正ループ
Step 7: Excel書き込み（Excel有りの場合）
Step 8: Markdown出力
Step 9: 完了レポート
```

### Step 0: 教員名・科目名の取得

最初に必ず教員名と科目名を確認する。これらはシラバスの基本であり、以降の全ステップで使用する。

プレーンテキストで質問する:
```
担当教員のお名前を教えてください:
科目名を教えてください:
```

### Step 1: Excelファイルの指定

プレーンテキストで質問する:
```
シラバスのエクセルテンプレートファイルのパスを教えてください
（ない場合は「なし」と入力してください）:
```

- パスが指定された場合 → Step 2a へ
- 「なし」の場合 → Step 2c へ

### Step 2a: Excel読み取り（Excel有りの場合）

同梱の読み取りスクリプトでデータを抽出する:

```bash
python3 ${CLAUDE_SKILL_DIR}/scripts/read_syllabus.py "<エクセルファイルパス>"
```

出力はJSON形式。スクリプトがエラーを返した場合（ファイル不在、シート不足など）はユーザーに伝えて対応を求める。

抽出した情報を整理して表示する:

```
=== シラバス基本情報 ===
教科名: [科目名]
科目番号: [番号]
科目区分: [区分]
授業形態: [形態]
単位: [単位]
開設学科: [学科]
対象学年: [学年]
開講期: [期]
週時間数: [時間]
担当教員: [教員名]

=== 到達目標 ===
（読み取った到達目標を表示）

=== ルーブリック ===
| 評価観点 | 理想的 | 標準的 | 未到達 |
|----------|--------|--------|--------|
| ... | ... | ... | ... |
```

**教員名・科目名の整合チェック**: Excel から読み取った値が Step 0 で入力された値と異なる場合、`AskUserQuestion` でどちらを使うか確認する:
- options: 「Step 0で入力した値を使う」「Excelの値を使う」

整合に問題がなければ、「この内容で合っていますか？修正があれば教えてください。」とプレーンテキストで確認する。

### Step 2b: 既存情報の利用確認（Excel有りの場合）

Excel に概要や授業計画が既に記入済みの場合、`AskUserQuestion` で確認する:

```
テンプレートに以下の既存情報が見つかりました:
- 概要: （記入済み / 空）
- 授業計画: （記入済み / 空）
- 評価割合: （記入済み / 空）
- 授業の属性: （記入済み / 空）
```

- options: 「既存情報をベースに修正・補完する」「既存情報を無視して新規作成する」

全て空の場合はこのステップをスキップする。

確認後 → Step 3 へ

### Step 2c: 対話で全情報収集（Excel無しの場合）

以下をプレーンテキストで順次収集する:

**1. 基本情報**
```
以下の基本情報を教えてください:
- 科目番号:
- 科目区分（例: 専門/必修）:
- 授業形態（例: 授業、演習）:
- 単位の種別と単位数（例: 履修単位: 1）:
- 開設学科:
- 対象学年:
- 開講期（例: 前期、後期、通年）:
- 週時間数:
- 教科書/教材:
```

**2. 到達目標**
```
この科目の到達目標を教えてください:
（複数ある場合は番号付きで入力してください）
```

**3. ルーブリック**

`AskUserQuestion` で方式を確認する:
- options: 「自分で入力する」「到達目標から自動生成してほしい」

「自分で入力する」の場合:
```
各評価観点について、理想的・標準的・未到達の基準を教えてください:

評価観点1:
- 理想的な到達レベル:
- 標準的な到達レベル:
- 未到達レベル:
```

「自動生成」の場合: 到達目標の内容からルーブリックを生成し、プレーンテキストで確認を取る。

収集完了後 → Step 3 へ

### Step 3: 授業構成パラメータの確認

以下を順に確認する（Excel有無にかかわらず必ず全項目を質問する）:

**授業回数**: プレーンテキストで質問する。
```
授業回数は何回ですか？（例: 15, 16, 30, 32）:
```

**授業構成案**: `AskUserQuestion` で授業スタイルを質問する。
- options: 「講義中心」「演習中心」「講義＋演習」「反転授業」

複合型（「講義＋演習」など講義と演習の両方を含むスタイル）が選ばれた場合、プレーンテキストで時間配分を質問する:
```
1コマ90分のうち、講義と演習の配分はどのくらいを想定していますか？
（例: 講義60分＋演習30分、講義45分＋演習45分）:
```

授業構成案と時間配分は、Step 5c の授業計画生成に反映する。各週の授業内容を記述する際に、構成に沿った流れ（例: 前半で概念を講義→後半で演習問題に取り組む）を意識して計画を組み立てる。

**使用する評価方法**: `AskUserQuestion`（multiSelect: true）で質問する。
- options: 「定期試験」「小テスト」「レポート・課題」「口頭発表」「成果物・実技」「ポートフォリオ」

選択された評価方法ごとのフォローアップ質問と重み付け方針の詳細は `references/evaluation-guide.md` を参照。

**MCC対応**: `AskUserQuestion` で質問する。
- options: 「MCCとの対応付けを行う」「MCCとの対応付けは不要」

### Step 4: MCC対応付け（オプション）

Step 3 で MCC 対応を選択した場合のみ実施。詳細手順は `references/mcc-guide.md` を参照。

### Step 5: 授業計画・出力項目の生成

到達目標・ルーブリック・MCCの分析結果をもとに、概要・注意点・授業計画・評価割合・授業の属性を生成する。各項目の生成ルールは `references/course-design-guide.md` を参照。

### Step 6: プレビューと修正

生成した全内容を `references/preview-format.md` のフォーマットに従ってMarkdown形式でプレビュー表示する。

`AskUserQuestion` で確認する:
- options: 「問題ない、確定する」「授業計画を修正したい」「評価割合を修正したい」「概要・注意点を修正したい」

修正が選択された場合は、プレーンテキストで具体的な修正内容を聞き、反映後に再度プレビューする。修正が完了するまで繰り返す。

### Step 7: ファイル出力（Excel有りの場合）

`AskUserQuestion` で出力形式を確認する:
- options: 「Markdown + エクセルの両方に出力する（推奨）」「エクセルのみに書き込む」「Markdownのみ出力する」

選択に応じた動作:
- 「Markdown + エクセルの両方」→ Excel書き込み（以下）を実行し、その後 Step 8 のMarkdown出力も実行する
- 「エクセルのみ」→ Excel書き込み（以下）を実行し、Step 8 はスキップする
- 「Markdownのみ」→ Excel書き込みをスキップし、Step 8 のMarkdown出力のみ実行する

「エクセルに書き込む」場合:

書き込み前にバックアップを `output/` ディレクトリに作成する:
```bash
mkdir -p output
cp "<元ファイル>" "output/<元ファイル名>.bak"
```

書き込みスクリプトを使用する:
```bash
python3 ${CLAUDE_SKILL_DIR}/scripts/write_syllabus.py "<エクセルファイルパス>" '<確定データJSON>'
```

スクリプトの仕様:
- 既存のセル書式（フォント、罫線、セル結合など）を保持する
- 値の書き込みのみ行う
- 書き込み対象: 科目情報・到達目標シート（到達目標）、教育方法等シート（概要・注意点）、授業計画シート、評価割合シート、授業属性シート

確定データJSONのフォーマットは `references/json-format.md` を参照。

Excel無しの場合はこのステップをスキップする。

### Step 8: Markdown出力

Markdownファイルは `output/` ディレクトリに保存する:
```bash
mkdir -p output
```
ファイル名: `output/[教科名].md`

Step 6 のプレビュー内容をそのままMarkdownファイルとして保存する。出力するセクション構成は Step 6 のプレビューフォーマットに従うこと（基本情報、到達目標、ルーブリック、概要、注意点、授業計画、評価割合、評価の計算方法・計算例、MCC対応、授業の属性）。

### Step 9: 完了レポート

```
=== シラバス作成完了 ===
教科名: [教科名]
担当教員: [教員名]
エクセル: [パス]（バックアップ: output/[ファイル名].bak）/ 未使用
Markdown: output/[教科名].md

生成した項目:
- 概要
- 注意点
- 授業計画（[N]週分）
- 評価割合
- 授業の属性
- MCC対応（該当する場合）
```

---

## リファレンス

エクセルテンプレートの構造とセル位置マッピングは `references/excel-template.md` を参照。
